spring:
  datasource:
    #    url: jdbc:h2:tcp://localhost/~/MenuIt;MVCC=TRUE
    url: jdbc:h2:tcp://localhost/~/MenuIt
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
#      ddl-auto: update
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace